apiVersion: v1
kind: Service
metadata:
  name: taskflow
  labels:
    app: taskflow
spec:
  selector:
    app: taskflow
    color: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
